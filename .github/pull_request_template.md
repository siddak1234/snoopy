## What this adds

<!-- 1–3 sentences: what service/change and why -->

## Checklist

- [ ] Follows `docs/REPO-STRUCTURE.md` (routes in `app/`, logic in `lib/`, UI in `components/`)
- [ ] `npm run lint` and `npm run typecheck` pass locally
- [ ] Ran the app locally (`npm run dev`) and tested the change end to end
- [ ] Prisma migration committed (if `prisma/schema.prisma` changed)
- [ ] New env vars added to `.env.example` and `lib/env.ts` — placeholders only, no real secrets in the diff

## Screenshots / demo

<!-- Screenshots of the solution page / dashboard views this PR adds -->
